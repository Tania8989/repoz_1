// using cycle for
#include <iostream>
#define _USE_MATH_DEFINES
#include "math.h"
using namespace std;
int main()
{
    //y=sin*|x|+cos^2*x       x[x1; x2]        h
    double y, x, x1, x2, h;
    cout << "Enter x1, x2, h \n";
    cin >> x1 >> x2 >> h;           //enter elements
    x = x1;

